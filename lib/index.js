/**
 * Module dependencies.
 */
var FaceitStrategy = require('./strategy');

/**
 * Framework version.
 */
require('pkginfo')(module, 'version');

/**
 * Expose `FaceitStrategy` directly from package.
 */
module.exports = FaceitStrategy;
